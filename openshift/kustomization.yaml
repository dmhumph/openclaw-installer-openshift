apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - clusterrole.yaml
  - clusterrolebinding.yaml
  - scc.yaml
  - scc-clusterrole.yaml
  - policy-configmap.yaml
  - secret.yaml
  - pvc.yaml
  - buildconfig.yaml
  - deployment.yaml
  - service.yaml
  - route.yaml
  # Observability (Phase 3)
  # Note: user-workload-monitoring.yaml targets openshift-monitoring namespace
  # and must be applied separately or with cluster-admin: oc apply -f openshift/user-workload-monitoring.yaml
  # observability-operator-subscription.yaml installs the operator (apply once)
  - uiplugin.yaml
  - openclaw-dashboard.yaml
