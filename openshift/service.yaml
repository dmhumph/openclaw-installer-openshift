apiVersion: v1
kind: Service
metadata:
  name: openclaw-installer
  namespace: openclaw-installer
  labels:
    app.kubernetes.io/name: openclaw-installer
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: openclaw-installer
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
