apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openclaw-installer-data
  namespace: openclaw-installer
  labels:
    app.kubernetes.io/name: openclaw-installer
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
